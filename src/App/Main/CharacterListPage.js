import React from 'react';

const CharacterListPage = ({
    id,
}) => {
    return (
        <>
            <h2 className="main__characters-container__list_page-title">Page {id}</h2>
        </>
    )
};

export default CharacterListPage;